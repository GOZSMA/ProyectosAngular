// import {PI, Person, generateRandonNumber} from './myModules';
// import {PI as MyPI } from './myModules';
import * as myCode from './myModules';

const user: myCode.Person = {
    id: 1,
    name: 'Brandon'
}

console.log(myCode.PI);

console.log(user);

console.log(myCode.generateRandonNumber());

